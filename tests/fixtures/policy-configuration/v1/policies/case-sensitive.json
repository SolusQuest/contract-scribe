{
  "schemaVersion": 1,
  "targetProfile": "profile.external-api",
  "defaultDecision": "optional",
  "rules": [
    { "id": "upper-app", "priority": 0, "decision": "required", "sourcePaths": { "include": ["src/App/**"] } }
  ]
}
