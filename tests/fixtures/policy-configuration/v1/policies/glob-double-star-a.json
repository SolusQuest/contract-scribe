{
  "schemaVersion": 1,
  "targetProfile": "profile.external-api",
  "defaultDecision": "optional",
  "rules": [
    { "id": "suffix-a", "priority": 0, "decision": "required", "sourcePaths": { "include": ["**/a"] } }
  ]
}
