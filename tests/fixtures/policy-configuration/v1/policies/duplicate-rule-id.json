{
  "schemaVersion": 1,
  "targetProfile": "profile.external-api",
  "defaultDecision": "optional",
  "rules": [
    { "id": "duplicate", "priority": 1, "decision": "required" },
    { "id": "duplicate", "priority": 2, "decision": "forbidden" }
  ]
}
